serial:
  lidar_baud_rate: 460800
  pc_transport_baud_rate: 921600
  port: null

display:
  range_m: 12.0
  update_rate_hz: 10.0

filters:
  minimum_distance_mm: 50
  maximum_distance_mm: 12000
  minimum_reflectivity: null

recording:
  root: data/raw

replay:
  speed: 1.0

map:
  resolution_m: 0.05
  width_m: 8.0
  height_m: 8.0
  occupied_update: 0.85
  free_update: -0.40

synthetic:
  scan_frequency_hz: 10.0
  angle_step_deg: 1.0
  noise_std_mm: 5.0
  random_seed: 7
