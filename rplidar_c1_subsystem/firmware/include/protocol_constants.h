#pragma once

namespace mars_rover::lidar::pc_transport {

// ESP32-to-PC frame constants are intentionally deferred until the transport
// phase documents field order, widths, signedness, units, and valid ranges.

}  // namespace mars_rover::lidar::pc_transport

namespace mars_rover::lidar::c1_protocol {

// Official SLAMTEC command bytes and packet layouts are intentionally not
// defined in Phase 0. Add them only from official protocol documentation.

}  // namespace mars_rover::lidar::c1_protocol
